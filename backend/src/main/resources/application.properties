spring.application.name=blog

# Server port
server.port=8080

# Pretty-print JSON responses (optional, nice for debugging)
spring.jackson.serialization.indent_output=true

# Logging level (optional, useful for development)
logging.level.org.springframework=INFO
